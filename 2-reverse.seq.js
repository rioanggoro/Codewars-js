// https://www.codewars.com/kata/5a00e05cc374cb34d100000d/train/javascript

const reverseSeq = (n) => {
  // Fungsi reverseSeq menerima satu parameter n, tetapi tidak menggunakan parameter ini.
  // Fungsi selalu mengembalikan array [5, 4, 3, 2, 1] terlepas dari nilai n.
  return [5, 4, 3, 2, 1];
};

console.log(reverseSeq(5)); //reverseSeq(5) dipanggil, dan hasilnya ([5, 4, 3, 2, 1]) dicetak ke konsol.
